# Dependências só de desenvolvimento/teste — não vão para o bundle da Vercel.
# Uso: pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0
httpx>=0.27.0
# scripts/gen_og_image.py — regenera a imagem de compartilhamento social
Pillow>=10.0.0
# Migrações de banco (docs/MIGRACOES.md). Fora do runtime: produção aplica as
# migrações no deploy, não no boot da aplicação.
alembic>=1.13.0
