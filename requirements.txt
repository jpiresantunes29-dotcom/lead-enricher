fastapi==0.115.0
uvicorn[standard]==0.30.0
jinja2==3.1.4
requests==2.32.3
brotlicffi>=1.1.0
beautifulsoup4==4.12.3
sqlalchemy==2.0.36
openpyxl==3.1.5
python-dotenv==1.0.1
dnspython==2.8.0
phonenumbers==8.13.49
ipwhois==1.3.0
python-jose[cryptography]>=3.3.0
# Declarada mesmo vindo junto do python-jose: services/crypto.py usa Fernet
# direto, e uma dependencia so transitiva some sem aviso quando a de cima muda.
cryptography>=42.0.0
psycopg2-binary>=2.9.0
stripe>=11.0.0
slowapi>=0.1.9
# FastAPI exige isto para rotas com Form()/UploadFile (routers/imports.py) —
# so vinha como dependencia opcional, ausente do ambiente serverless.
python-multipart>=0.0.9
